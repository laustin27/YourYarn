import {StyleSheet} from 'react-native';

const buttonStyles = StyleSheet.create({
    primary: {
        fontSize: 20,
        width: '80%',
        alignItems: 'center',
        paddingVertical: 12,
        borderRadius: 5,
        backgroundColor: 'red',
    },
    primaryButtonText: {
        
    }
});

export {buttonStyles}